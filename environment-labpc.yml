# Lab PC (Linux) environment — install this on the lab server.
# conda env create -f environment-labpc.yml
name: mydropbox-labpc
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - xarray
  - numpy
  - matplotlib
  - pandas
  - pyyaml
  - pip
  - pip:
      - -e .
